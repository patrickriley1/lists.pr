This is a music ranking app I've been working on using React and Vite.

## Environment variables

Server (`server.js`) expects:

- `DATABASE_URL`
- `TOKEN_SECRET`
- `SPOTIFY_CLIENT_ID`
- `SPOTIFY_CLIENT_SECRET`
- `PORT` (optional, defaults to `4000`)
